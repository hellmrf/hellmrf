# CV em PDF (LaTeX)

Para compilar o CV em PDF, utilize o `latexmk`:

```bash
cd PDF
latexmk -xelatex main.tex
```

Nos dois casos, o arquivo `main.pdf` será gerado no mesmo diretório do arquivo `main.tex`.
